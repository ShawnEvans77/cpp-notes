#include <iostream>

int main() {
    // references - don't use derefs for reading & writing
    // cant be changed to ref something else
    // must be initialized at decleration

    // must go through deref operator for reading/writing
    // can be changed to point somewhere else
    // can be declared uninitialized (will contain garbage address)

    // reference variables requires initializers

    
}