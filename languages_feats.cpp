#include <iostream>

int main() {
    // core feature - a basic building block that builds up the c++ language. how to define a variable, how to define a function,
    // control structures, variables. and so on.

    // types, the rules for how you can define a function.
    // core features for how we build c++ programs.

    // the standard library is highly specialized components. specialized components that we can use in regular c++ programs.
    // we want to use the c++ programming language to build features of our own.
    // std::cout is a wrapper for iostream, which lets use it relatively easily in our program.

    // the STL is part of the c++ standard library, but its a collection of container types. think of them as collections
    // of things.

    // we have algorithims. we have collections. we have specialized types called iterators that allow us to navigate through
    // this contianers. you can think of the STL as a highly specialized part of the C++ standard library. standard template library.
}